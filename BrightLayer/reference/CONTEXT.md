# Reference Context

## Purpose

This area contains reference inputs used to understand the existing BrightLayer work.

- `parameters/` contains parameter lists and CSV reference files.
- `ontology/` contains ontology reference data.

## Constraints

Reference files are inputs and historical evidence. Do not modify identifiers or treat a reference artifact as executable production code without checking the owning application or development script.
